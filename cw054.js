const min = (list) => Math.min(...list)
const max = (list) => Math.max(...list)

/*
const min = (list) => {
    list.sort((a, b) => (a - b))
    return list[0]
}

const max = (list)=>{
    list.sort((a, b) => (b - a))
    return list[0]
}*/
